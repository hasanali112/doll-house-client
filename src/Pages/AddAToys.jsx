import React from 'react';
import useTitle from '../hooks/useTitle';

const AddAToys = () => {
    useTitle('Add A Toys')
    return (
        <div>
            <h1>Add a toy</h1>
        </div>
    );
};

export default AddAToys;